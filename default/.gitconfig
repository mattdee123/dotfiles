[user]
	name = Matt Dee
	email = mattdee123@gmail.com
[core]
	editor = vim
	excludesfile = /Users/mattdee/.gitignore_global
[credential]
	helper = osxkeychain
[push]
	default = current
[alias]
    remove = remote
    url = !echo "$(git config --get remote.origin.url | sed s/.git$//)"
    hub = !open "$(git url)"
    cmp = !open "$(git url)/commit/$(git rev-parse HEAD)"
    amend = commit --amend
    b = branch
